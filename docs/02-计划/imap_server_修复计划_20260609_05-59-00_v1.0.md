# IMAP 服务器审查报告问题修复计划书

| 字段     | 值                           |
|----------|------------------------------|
| 标题     | IMAP 服务器审查报告问题修复计划 |
| 日期     | 2026-06-09                   |
| 时间     | 05:59:00                     |
| 版本     | v1.0                         |
| 作者     | Cline                        |
| 模块     | imap-server                  |
| 状态     | 已批准                       |
| 关联文档 | `docs/04-模块/imap_server_审查报告_20260609_v1.0.md` |

## 1. 目标与变更范围

### 1.1 目标
修复审查报告中提出的全部 10 个问题（BUG-1 ~ BUG-7），同时保留 fork 项目的所有改进。

### 1.2 要改什么
| 序号 | 文件 | 修改内容 |
|------|------|----------|
| 1 | `server/listen/imap_server/imap_server.go` | Caps 能力集合添加 IDLE/NAMESPACE/UIDPLUS/MOVE/CHILDREN |
| 2 | `server/listen/imap_server/session_idle.go` | 重写 IDLE 连接管理（mutex/正确清理/mailbox跟踪/实际消息数） |
| 3 | `server/dto/parsemail/email.go` | formatContent 添加 Content-Disposition attachment 检查 |
| 4 | `server/listen/imap_server/imap_server_test.go` | 补充能力测试用例 |

### 1.3 不改什么
- fork 的 `stripHTMLTags` 改进（保留）
- fork 的 XSS 策略扩展（保留）
- fork 的 `multipart/related` 双重遍历修复（保留）
- fork 的 MOVE/COPY UeId 精确匹配（保留）
- fork 的 COPY 重复检查（保留）
- fork 的日志系统改进（保留）
- fork 的 group.go 改进（保留）

## 2. 影响评估与依赖

### 2.1 影响范围
| 修改 | 影响模块 | 风险 |
|------|----------|------|
| Caps 能力公布 | IMAP 客户端连接 | 低：仅声明式修改 |
| IDLE 重写 | IMAP IDLE 连接管理 | 中：核心逻辑变更 |
| formatContent 修复 | 邮件解析 | 低：纯增量检查 |
| 测试补充 | 测试套件 | 无：仅测试代码 |

### 2.2 依赖关系
- `session_idle.go` 依赖 `group.GetGroupStatus`（已存在）
- `email.go` 需抽取 `getFileName` 辅助函数（参照上游）

## 3. 实施步骤与检查点

1. 修复 `imap_server.go` Caps → 编译验证
2. 重写 `session_idle.go` → 编译验证
3. 修复 `email.go` formatContent → 编译验证
4. 补充测试用例 → 编译验证
5. 全量编译验证 → `cd server && go build ./...`

## 4. 回滚方案与失败判定

### 4.1 回滚方案
所有修改通过 git 管理，可通过 `git checkout -- <file>` 回滚单个文件。

### 4.2 失败判定
- 编译失败：立即停止，修复编译错误后继续
- 测试失败：分析失败原因，确定是否为预期行为变更

## 5. 所需文档清单

| 文档 | 路径 |
|------|------|
| 代码修改文档（Caps） | `docs/04-模块/imap_server/imap_server_Caps_修正_20260609_05-59-00_v1.0.md` |
| 代码修改文档（IDLE） | `docs/04-模块/imap_server/session_idle_修正_20260609_05-59-00_v1.0.md` |
| 代码修改文档（formatContent） | `docs/04-模块/parsemail/formatContent_修正_20260609_05-59-00_v1.0.md` |

## 6. 审批与共识

- 审查报告已确认全部问题
- 修复方案已确认保留 fork 改进
- 计划已获批准，进入实施阶段