# 变更日志汇总 v2.7

| 字段 | 值 |
|---|---|
| 版本 | v2.7 |
| 日期 | 2026-08-15 |
| 时间 | 19-43-00 |
| 作者 | PMail Team |

---

## 变更记录

### 2026-08-15 母项目改进全面核查与补齐移植（v2.6 收尾）

对母项目（上游 `github.com/Jinnrry/pmail`）相对现有项目（`github.com/ffyuhf/pmail`）执行**全量系统核查**：import 归一化后 `diff -r` 全部 server 同构 Go 文件（67 个实质差异文件）与根级构建文件，提取母项目独有非注释代码行 939 行逐行判定，三分类处置（A 移植 / B 保留 / C 忽略），**零未处置**。

| 编号 | 变更项 | 内容 |
|------|--------|------|
| MIG-10 | genSQL 系统文件夹双条件查询（listview） | `services/list/list.go`：Status=已删除/草稿/垃圾 → `(ue.status=? or ue.group_id=Deleted/Drafts/Junk)`；发件箱 → `(type=? or ue.group_id=Sent)`；收件箱/全部邮件视图 → `(group_id=0 or group_id=INBOX)`。与上轮 MIG-05 doMove 强配套（查询侧此前缺失）。保留自有修复：GroupId>0 免 status 过滤、LIKE 转义、`ue_id` 查询列；新增 `TestGenSQL` 6 子用例（含防回归锁定） |
| MIG-11 | db SQL 调试开关 | `db/init.go`：`ShowSQL(config.Instance.LogLevel == "debug")` 替代固定 false |
| MIG-12 | 附件隔离测试 | `dto/parsemail/email_test.go` 新增 `TestHtmlTxtAttachment`（母项目逐字移植，PASS） |
| MIG-13 | Gmail 收信集成测试 | `listen/smtp_server/read_content_test.go` 新增 `TestGmail`（ARC/DKIM/中文编码真实样例，母项目逐字移植） |
| MIG-14 | IMAP 测试基础设施与工厂 | `imap_server.go` 抽取 `newIMAPServer()` 工厂（生产逻辑原样，保留 Caps/remoteAddr）；`imap_server_test.go` 合并移植母项目基础设施：ROOT_PATH 自适应、随机端口监听、`seedIMAPTestData` 种子数据、完整 `TestIDLE`（原为空实现）、新增 `TestIdleNotice`/`readTaggedLine`/`waitIdleConnectionsDeleted`；保留现有 TestCapability 的 BUG-1/4/5/6/7 断言 |

**核查结论**（详见核查报告）：67 个实质差异文件全部处置——A 类 5 项本次移植、B 类 54 文件为现有项目自有增强/领先实现（bcrypt、ratelimit、DSN、结构化日志、ue_ids 精确化、越权修复、Caps 修复、api_token 等全部保留未动）、C 类 13 项无实质差异；上轮 MIG-01~09 抽样复核在位；**除上述 5 项外母项目不存在其他未移植改进**，母项目改进已全部移植完毕。前端 `fe/`（JS 旧版）按共识排除。

### 涉及文档清单

- 计划书: `docs/02-计划/母项目改进全面核查_计划_20260815_19-05-00_v1.0.md`
- 核查报告: `docs/03-设计/01-架构设计/母项目改进移植完整性核查报告_20260815_19-43-00_v1.0.md`
- 修改文档:
  - `docs/04-模块/listview/list_genSQL系统文件夹查询_修正_20260815_19-43-00_v1.0.md`
  - `docs/04-模块/db/db_ShowSQL调试开关_修正_20260815_19-43-00_v1.0.md`
  - `docs/04-模块/parsemail/parsemail_TestHtmlTxtAttachment测试_新增_20260815_19-43-00_v1.0.md`
  - `docs/04-模块/smtp_server/smtp_server_TestGmail测试_新增_20260815_19-43-00_v1.0.md`
  - `docs/04-模块/imap_server/imap_server_测试基础设施与工厂_新增_20260815_19-43-00_v1.0.md`
- 计划索引: `docs/02-计划/03-索引/计划总览_按项目_20260815_19-43-00_v1.2.md`

### 验证记录

- `go build ./...`（19:38，dist 占位方案同 v2.6）：全量通过（exit 0），验证后占位已清理。
- `go test ./...`（19:42 复验）：含测试包 12 个全 PASS（含新增 `TestGenSQL` 6 子用例、`TestHtmlTxtAttachment`）；46 包 ok/no-test-files。
- 失败包 7 个**均为预存环境性失败，经 git stash 基线逐一对比确认与本次零交集**：
  - `根包`、`listen/http_server`、`res_init`（`go:embed dist/*` 无匹配——前端产物未构建，v2.6 已知预存，连锁影响根包/res_init 编译）
  - `listen/imap_server`（config.json 未找到）、`listen/pop3_server`（SQLite 临时目录）、`listen/smtp_server`（dkim.priv 未找到）、`services/group`（group_test.go:14 对 nil Instance 调用 ShowSQL——基线同点位失败）
- 母项目函数逐字保真校验：`TestHtmlTxtAttachment`、`TestGmail`、`TestMain`、`seedIMAPTestData`、`TestIDLE`、`TestIdleNotice`、`readTaggedLine`、`waitIdleConnectionsDeleted` 共 8 个函数段 diff 逐字一致。
- gofmt / go vet：全部修改文件通过（`imap_search.go` gofmt 告警为预存，未触碰）。

### G3 审计记录（2026-08-15 19:50）

| 检查域 | 结果 |
|--------|------|
| A1 阅读完整性（G1） | 通过（阅读清单/确认声明已于规划阶段输出） |
| A2 文档合规 | 通过：计划书六章齐备、5 份修改文档四章齐备、核查报告含结论与验证记录、变更日志 v2.7 版本连续（v2.6→v2.7）、索引 v1.2 已登记 |
| A3 目录与命名 | 通过：文档归档于 docs/02-计划、03-设计、04-模块 正确子目录；文件名含实体/类型/日期/时间/版本 |
| A4 版本与归档 | 通过：v2.6→v2.7、v1.1→v1.2 连续；变更日志含完整字段 |
| A5 代码修改 | 通过：5 个修改单元均为最小化合并式移植；自有增强（LIKE 转义/ue_id/GroupId>0 修复/Caps 断言/bcrypt 等 B 类 54 文件）经 diff 与 TestGenSQL 锁定未删改；母项目函数逐字保真 |
| 审计结论 | **通过（G3 放行）** |
| 整改措施 | 无 |

### 审计人签名

- 实施负责人：Code 模式代理（已完成全量核查、移植、验证与留痕）
- 审计人：Code 模式代理（G3 审计通过）
- 项目负责人（用户）：已批准计划书（2026-08-15 19:08，含全面核查口径与前端排除共识）

---

> 历史记录（v2.6 及更早）见 `docs/04-模块/01-变更日志/` 下对应版本文件，本文件仅追溯至 v2.7 基线。
